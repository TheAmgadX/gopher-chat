`/username` -> rename the user.
`/join` -> join a chatting room if exists, if not making a new room.
`/rooms` -> list the available rooms.
`/quit` -> quit the current room.
`/msg` -> send a message to the users in the rooms.